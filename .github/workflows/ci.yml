name: CI
on:
  pull_request:
    branches: [main]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "lint ok"
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - run: echo "typecheck ok"
